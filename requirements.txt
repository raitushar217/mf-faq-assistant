# MF FAQ Assistant — requirements.txt
# Lean dependencies for Render (FastAPI Backend + Retrieval)
# Stay under 512MB RAM by avoiding torch/sentence-transformers.

# ---------------------------------------------------------------------------
# Scraping & Core
# ---------------------------------------------------------------------------
requests==2.31.0
playwright==1.44.0

# ---------------------------------------------------------------------------
# Vector Store (Lean - uses ONNX for default embeddings)
# ---------------------------------------------------------------------------
chromadb==0.4.22
onnxruntime==1.17.1          # Required for Chroma's default embedding function

# ---------------------------------------------------------------------------
# API & LLM
# ---------------------------------------------------------------------------
groq==0.9.0
fastapi==0.111.0
uvicorn==0.30.0
python-dotenv==1.0.1
httpx==0.27.2               # Required by groq client
