# MF FAQ Assistant — requirements.txt
# Versions pinned as specified in architecture.md
# Install: pip install -r requirements.txt
# After install: playwright install chromium

# ---------------------------------------------------------------------------
# Phase 4.0 — Scraping
# ---------------------------------------------------------------------------
requests==2.31.0            # non-Groww URLs (AMFI, SEBI, HDFC, CAMS, etc.)
playwright==1.44.0          # Groww URLs (headless Chromium, networkidle)

# ---------------------------------------------------------------------------
# Phase 4.2 / 4.3 — Embedding + Vector Store
# ---------------------------------------------------------------------------
sentence-transformers==2.6.1  # BAAI/bge-small-en-v1.5 — NEVER substitute
chromadb==0.5.23              # local PersistentClient only — no Chroma Cloud
                              # (0.5.x: pre-built wheel for Python 3.12/Windows)

# ---------------------------------------------------------------------------
# Phase 6 — LLM Generation
# ---------------------------------------------------------------------------
groq==0.9.0                 # Groq API client (llama3-8b-8192)

# ---------------------------------------------------------------------------
# Phase 8 — FastAPI Backend
# ---------------------------------------------------------------------------
fastapi==0.111.0            # async REST API (/chat, /health)
uvicorn==0.30.0             # ASGI server (startup: download_artifact + serve)

# ---------------------------------------------------------------------------
# Shared utilities
# ---------------------------------------------------------------------------
python-dotenv==1.0.1        # loads .env → GROQ_API_KEY, CHROMA_PERSIST_DIR, etc.
httpx==0.27.2               # groq==0.9.0 compatibility (0.28+ breaks proxies arg)
